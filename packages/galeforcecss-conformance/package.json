{
  "name": "@cx/galeforcecss-conformance",
  "version": "0.0.0",
  "private": true,
  "description": "Conformance harness: oracle (Tailwind 3) vs. GaleforceCSS.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "galeforce-conformance": "./dist/cli.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "update": "vitest run --update",
    "smoke": "tsx src/smoke.ts",
    "bench": "tsx src/bench.ts"
  },
  "dependencies": {
    "@cx/galeforcecss-config-loader": "workspace:*",
    "@cx/galeforcecss-oracle": "workspace:*",
    "@cx/galeforcecss": "workspace:*",
    "postcss": "^8.4.47"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.5"
  }
}
